#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - prints is positive, negative
  * or zero
  *Return: returns zero if program exits correctly.
   */
int main(void)
{
	char c = 'a';
	while
		(c <= 'z')

	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
