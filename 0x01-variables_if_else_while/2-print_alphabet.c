#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - 
  * 
  *Return: returns zero if exits correctly.
   */
int main(void)
{
char r :
for (r = 'a' : r <= 'z' : r ++)
{
putchar(c);
++;
}
putchar('\n');
return (0);
}
