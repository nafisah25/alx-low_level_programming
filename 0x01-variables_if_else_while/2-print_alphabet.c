#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
  * main - prints positive, negative
  * or zero
  *Return: returns zero if run well.
   */
int main(void)
{
	char c = 'a';
	while
	(c <= 'z')

	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
