#include <stdio.h>

/**
  * main - prints positive, negative
  * or zero
  * Return: returns 0 if exits successfully.
  */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf('\n');
	return (0);
}
