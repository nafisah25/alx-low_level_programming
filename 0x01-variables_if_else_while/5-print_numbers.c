#include <stdio.h>

/**
  * main - prints positive, negative
  * or zero
  * Return: returns 0 if exits successfully.
  */
int main(void)
{
	int c = 0
		;
	while (c <= 9)
	{
		printf("%i", c);
		c++;
	}
	printf("\n");
	return (0);
}
